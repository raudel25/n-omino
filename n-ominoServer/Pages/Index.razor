@page "/"
@using Rules
@using InteractionGui
@using Table
@using Game
@using Player

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.

<a href="/Game">
    <button>Game</button>
</a>
<button @onclick="() => { a=true;}">Game1</button>

<BuildGameGui DefaultValue="2" Game="game"></BuildGameGui>

<GameGui Judge1="_judge"></GameGui>

<button @onclick="() => { _judge=new Judge<int>(null,game[0].Rules,
    game[0].Initializer.StartGame(new List<(int, int)>() { (0, 0), (1, 1), (2, 2), (3, 3) }),
    _players,game[0].Print);}">lnksv</button>

@code
{
    private bool a;

    private List<BuildGame<int>> game =new List<BuildGame<int>>();

    private Judge<int> _judge = Test.Game();

    private Player.Player<int>[] _players = new[] {new RandomStrategyPlayer<int>(new IStrategy<int>[]{}, new ICondition<int>[]{}, new GreedyPlayer<int>(),0), 
        new RandomStrategyPlayer<int>(new IStrategy<int>[]{}, new ICondition<int>[]{}, new GreedyPlayer<int>(),1),
        new RandomStrategyPlayer<int>(new IStrategy<int>[]{}, new ICondition<int>[]{}, new GreedyPlayer<int>(),2), 
        new RandomStrategyPlayer<int>(new IStrategy<int>[]{}, new ICondition<int>[]{}, new GreedyPlayer<int>(),3)};
    
}